include "reference.conf"

# DEVELOPMENT configuration
# ---------------------------------------------------------------------------------------------------------------------
db.default.url="jdbc:h2:mem:play;DB_CLOSE_DELAY=-1"
db.default.driver=org.h2.Driver
db.default.jndiName=DefaultDS
db.default.leakTimeMinutes=2

# LOCAL PRODUCTION TESTING 
# ('sbt:~ runProd -Dplay.evolutions.db.default.autoApply=true')
# ---------------------------------------------------------------------------------------------------------------------
# db.default.url="jdbc:h2:./data/play"
production.mockDB = true



# APPLICATION SETTINGS (template only)
# ---------------------------------------------------------------------------------------------------------------------

play.http.secret.key = "change-me___change-me___change-me___change-me___change-me___change-me___change-me"

df {
	# specify upload directory with trailing slash
	upload_directory = "data/"
	
	# base url for data foundry service (can include port)
	base_url = "http://localhost:9000"

	links {
		organization = "Example Organization"
		about = "https://example.com/about"
		contact = "https://example.com/contact"
		privacy = "https://example.com/privacy"	
		scientific_integrity = "https://example.com/integrity"
		organization_logo = "https://example.com/org-logo"
		teams_community = ""	
	}

	users {
		# list of admin user email addresses
		admins = ["admin1@example.org"]
		
		# list of librarians user email addresses
		librarians = ["libr1@example.org"]
		
		# list of moderator user email addresses
		moderators = ["mod1@example.org"]
		
		# list of reviewers' email addresses
		reviewers = ["reviewer1@example.org"]
	}

	sso {
		client = ""
		secret = ""
		tenant = ""
		discovery = ""
	}

  telegram {
      bot_name = ""
      token = ""
  }

	msgraph {
		from   = ""
		client = ""
		secret = ""		
		tenant = ""
	}

	keys {
		registration = [""]
		
		entity = ""
		project = ""
		api = ""
		auth.api = ""
	
		v2 {
			# only for trusted parties, do you need a formal agreement?
			multi.api = [""]
			
			# convention: service name in first 8 characters of api key
			user.api = [""]
		}
	}
	
	#feedback_link = ""

	# OOCSI server for incoming and outgoing events	
	oocsi_server = ""
	
	# 3rd party services
	vendor {
		# for errors and email logs
		slack.channel = "https://hooks.slack.com/services/TTTT..."
	
		# Fitbit App Setting
		#fitbit.id = "xxxxxxx"
		#fitbit.secret = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
		
		# GoogleFit API Setting
		#googlefit.id = "111111111111-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com"
		#googlefit.secret = "xxxxx-xxxxx_xxxxxxxxx-xx"
	}

  # processing with Python
	processing.python = "python3"	
}

commands {
	convert = "/opt/local/bin/convert"
} 

# SERVER CONFIGURATION
# ---------------------------------------------------------------------------------------------------------------------

play.modules {
  enabled += modules.SecurityModule
}

# ---------------------------------------------------------------------------------------------------------------------

play.http.session {
  # Whether the secure attribute of the cookie should be set to true
  # development: false, production: true
  secure = false

  # The domain to set on the session cookie
  # If null, does not set a domain on the session cookie.
  # development: "localhost", production: "id.tue.nl"
  domain = "localhost"

  # The max age to set on the cookie.
  # If null, the cookie expires when the user closes their browser.
  # An important thing to note, this only sets when the browser will discard the cookie.
  # 43200000 = 12 hours
  maxAge = 43200000

  # Whether the HTTP only attribute of the cookie should be set to true
  httpOnly = true

  # The value of the SameSite attribute of the cookie. Set to null for no SameSite attribute.
  # Possible values are "lax" and "strict". If misconfigured it's set to null.
  sameSite = "strict"
}

play.i18n.langs = [ "en", "nl", "de" ]

# upload file size limitation
play.http.parser.maxDiskBuffer = 30MB
play.http.parser.maxMemoryBuffer = 1024K
parsers.anyContent.maxLength = 30MB
